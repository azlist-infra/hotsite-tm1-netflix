/**
 * Arquivo de fontes para geração de tipos do Chakra UI
 * Este arquivo é usado apenas pelo CLI do Chakra
 * O arquivo fonts.ts real usa next/font/google e next/font/local
 */

const fonts = {
    primaryFont: { style: { fontFamily: "'Roboto', sans-serif" } },
    secondaryFont: { style: { fontFamily: "'Roboto', sans-serif" } },
    customFont: { style: { fontFamily: "'VCR OSD Mono', monospace" } }, // Mock para VCR OSD Mono
}

export default fonts

